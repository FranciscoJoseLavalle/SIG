{% extends 'base.html.twig' %}

{% block stylesheets_page %}
	<link rel="stylesheet" type="text/css" media="screen" href="{{ asset('build/css/fullcalendar.css') }}"/>
{% endblock %}

{# {% block path %}
	<div class="page-bar" style="margin-bottom: 10px">
		<ul class="page-breadcrumb">
			<li>
				<a href="{{ path('index') }}">Home</a>
				<i class="fa fa-circle"></i>
			</li>
			<li>
				<a href="{{ path('calendario_show') }}">Calendario</a>
			</li>
		</ul>
	</div>
{% endblock %} #}
{% block body %}
	{{ parent() }}
	{% for label, messages in app.flashes() %}
		{% for message in messages %}
			<div class="alert alert-{{ label }} fade in">
				<button class="close" data-dismiss="alert">×</button>
				{{ message }}
			</div>
		{% endfor %}
	{% endfor %}
	<sectionid="widget-grid" class="">
		<div
			class="row">
			{# <!-- NEW WIDGET START --> #}
			<div class="col-md-12">
				<div class="portlet light bordered">
					<div class="portlet-title">
						<div class="caption font-dark">
							<i class="fa fa-calendar font-dark"></i>
							<span class="caption-subject bold uppercase">
								Calendario
							</span>
						</div>
					</div>
					<div class="portlet-body form">
						<div class="row">
							<div class="col-sm-12">
								<p>Leyenda:
									<span class="label label-primary">Cheques</span>
								</p>
								<div id='calendar' style="width: 60%; text-align: center; margin: auto;"></div>
							</div>
						</div>
					</div>
				</div>
			</div>
			<!-- WIDGET END -->
		</div><!-- end row --></section><!-- end widget grid -->{% endblock %}
{% block javascripts %}
	{{ parent() }}<script src="{{ asset('build/js/public/calendario.js') }}" type="text/javascript"></script>
{% endblock %}
